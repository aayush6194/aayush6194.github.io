let URL  = "https://finapp.aayushh.com";
URL  = "http://localhost/api";

const API = {
  //User
  visit: URL + "/visited",
  signin: URL + "/signin",
  auth: `${URL}/auth`,
  message: `${URL}/message`,
  like: `${URL}/like`
 }

export { URL , API };